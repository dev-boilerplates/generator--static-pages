- var p = museummadedigital
header
  +navigation--mobile()
+navigation--desktop()

+pageHero(p.body.title, p.images.hero)

+section
  +block
    p Entry Text
   
+section("images")
  +block
    +imageCollectionFromTo(p.images.collection, 0, p.images.collection.length - 1)

+section
  +projects-link("is-centred")